# Claude Code project config
.claude/

# Benchmarks (cloned repos, large data)
benchmarks/

# Run outputs (traces, results, binaries)
runs/

# Agent workspace (scratch files, binaries)
workspace/

# Agent memory (per-instance)
memory/

# Measured-performance store (per-instance calibration data)
measurements/

# Per-instance hardware profiles (microbench JSONs) — generated per machine
agent/tools/modeling/configs/hw_profiles/

# Ad-hoc experiment scripts and standalone sims (leading underscore convention)
agent/tools/modeling/_*.py
!agent/tools/modeling/__init__.py

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Personal dev log (not part of the public repo)
DEV_NOTES.md
eval/PLAN.md

# Local chat configs — contain internal endpoints / paths
chat.yaml
chat.*.yaml

# Eval sandbox + ad-hoc speedup playground (large binaries / nsys reports)
eval/example_speedup/

# webui frontend build artefacts + dev deps
webui/frontend/node_modules/
webui/frontend/dist/
